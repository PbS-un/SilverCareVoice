# 銀髮一句通 SilverCare Macau — Server 環境變數範本
# 複製為 server/.env（已 gitignore），填入真實密鑰。密鑰永不下發前端、永不進 git。

# DeepSeek API 密鑰（留空 → /api/ai/chat 回 {provider:'local', reason:'no_key'}，由客戶端本地引擎處理）
DEEPSEEK_API_KEY=

# DeepSeek 端點與模型
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# Server 埠（web dev proxy 轉發目標）
PORT=8787

# 雙裝置同步配對 token（/sync/* 與 WS /ws 的最低限度鑑權）。
# 留空 → 每次啟動自動生成並打印到啟動日誌（Demo 性質，見 server/README.md）。
# 固定 token 請填入任意長隨機字串；第二裝置以 URL ?syncToken=<token> 配對。
SYNC_TOKEN=
