# SilverCare Voice — web 前端環境變量樣板（Task #2：可配置 Supabase 雲端後端）
#
# 複製為 .env（或 .env.production）並填入實際值；構建時由 Vite 注入。
# 全部留空（預設）→ 本地模式：行為與歷史版本完全一致
# （相對路徑 /api、/sync、/ws，dev 經 vite.config.ts proxy 轉發 localhost:8787）。

# Supabase Edge Function 完整 base URL（雲端模式開關：非空即啟用）
# 形如 https://<project-ref>.functions.supabase.co/silvercare
# 屬非機密資訊（URL 最終會出現在瀏覽器網絡請求中）。
VITE_SUPABASE_URL=

# Supabase anon public key —— 屬公開層級（設計上可下發到前端，受 RLS / apikey 校驗約束），
# 絕不是 service_role key！切勿在此放置 service_role 或任何機密密鑰。
VITE_SUPABASE_ANON_KEY=

# 選填：Supabase 項目根 URL（Realtime 連線用）。
# 缺省時自動從 VITE_SUPABASE_URL 推導（.functions.supabase.co → .supabase.co），
# 僅在推導不適用時才需顯式設置。
# VITE_SUPABASE_PROJECT_URL=https://<project-ref>.supabase.co
